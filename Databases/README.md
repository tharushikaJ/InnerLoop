# InnerLoop Database

PostgreSQL database assets for InnerLoop.

The proposal specifies PostgreSQL, SQLAlchemy ORM, and Alembic migrations. The `migrations/` directory is reserved for schema migration scripts and `seed/` for development seed data.
